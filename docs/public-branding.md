# Public branding notes

Use this file as the source of truth for public GitHub presentation.

## Repository settings

- Description: `Bilingual, reproducible Fanqie export-to-import pipeline with resumable chapter fetch, EPUB validation, deduplicated manifest import, and a local signing service.`
- Homepage: `https://github.com/Syking-of-Sky/fanqie-novel-export-pipeline/blob/main/docs/2026-08-24_fanqie-export-pipeline-report.md`
- Topics: `python`, `java`, `spring-boot`, `epub`, `unidbg`, `mermaid`, `automation`, `workflow`, `data-pipeline`, `documentation`

## Social preview copy

### Chinese

- Title: `番茄小说导出与导入流程`
- Subtitle: `从分享链接到 TXT/EPUB，再到 manifest 幂等导入的可复现工具链`
- Short blurb: `一个中英双语、可复现、可审计的小说导出流程仓库：目录读取、章节断点续传、去重导出、EPUB 校验与服务器导入全部串起来。`

### English

- Title: `Fanqie export/import pipeline`
- Subtitle: `A reproducible workflow from share URL to TXT/EPUB and idempotent manifest import`
- Short blurb: `A bilingual, reproducible workflow repository for chapter fetching, resumable caching, deduplicated export, EPUB validation, and server-side import.`

## Suggested launch copy

### Chinese

`公开整理了一个番茄小说导出流程仓库：从分享链接 / book_id 开始，走目录、批量章节、cache 断点续传、去重、TXT/EPUB、校验、manifest 和服务器幂等导入。代码、配置模板和双语说明都在仓库里，正文与凭据不入库。`

### English

`Published a bilingual workflow repository for a reproducible Fanqie export/import pipeline: share URL or book_id -> catalog -> batch chapters -> resumable cache -> dedup -> TXT/EPUB -> validation -> manifest -> idempotent import.`
